name: testing  input
on:
  workflow_dispatch: 
    inputs:
      Environment:
        required: true
        description: "enter do you think True or false"
        type: boolean
jobs:
  test:
    runs-on: macos-13
    steps:
      - name: printing name
        run: echo "hi ${{ inputs.Environment }}"

  
