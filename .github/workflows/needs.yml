name: Job Dependency

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build completed"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Test completed"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo "Deploy completed"
