name: testing string input
on:
  workflow_dispatch: 
    inputs:
      Environment:
        required: true
        description: "enter your name"
        type: string
jobs:
  test:
    runs-on: macos-13
    steps:
      - name: printing name
        run: echo "hi ${{ inputs.Environment }}"

  
