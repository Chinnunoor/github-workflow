name: greetings

on:
  push:

env:
  Name: noor

jobs:
  job_name:   # 👈 must be indented
    runs-on: ubuntu-latest

    steps:
      - name: printing greetings
        run: echo "welcome to team $Name"
