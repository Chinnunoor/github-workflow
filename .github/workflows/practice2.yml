name: practice2
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checksout repo
        uses: : actions/checkout@v4
      - name: printing req output
        run: |
          ls
          echo "Starting Maven workflow"
          echo "Level 1 completed"
